// regex for validation
export const EMAIL_REGEX = /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,9}$/i;

// API paths
export const API_URL = 'https://mplay.elemental.tv/v1/';
export const LOGIN_PATH = 'users/login';

// route paths
export const LIVE_PAGE_PATH = '/channels';
export const ON_RECORD_PAGE_PATH = '/on-record';

// constant numbers
export const SELECTED_LIVE_TAB = 0;
export const SELECTED_ON_RECORD_TAB = 1;
export const MINUTE_TIME_IN_MILLISECONDS = 60000;
